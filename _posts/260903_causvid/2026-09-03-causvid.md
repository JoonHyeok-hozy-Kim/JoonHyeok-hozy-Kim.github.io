---
layout: post
title: From Slow Bidirectional to Fast Autoregressive Video Diffusion Models (CausVid)
date: 2026-09-03 11:00:00
description: Yin et al.
tags: video DMD
categories: ar_diffusion_model
# pdf: _posts/250908_style_gan/slides_style_gan.pdf
pretty_table: true
---

[Yin et al. 2025](https://arxiv.org/abs/2412.07772)

[code](https://causvid.github.io/)

<br>

---

### Hozy Summary
- Problem)
  - Video diffusion models with the DiT backbone struggle in interactive applications
    - Why?) Bidirectional Attention Dependencies
      - Desc.)
        - DiT is bidirectional in nature.
        - Thus, each frame attend should attend the whole video, including future frames.
        - Hence, interactive generation is unavailable.
        - Also, DiT's full attention is too costly.
- Sol.)
  - Adapt a pretrained bidirectional DiT to an AR transformer
  - Extend DMD to videos to reduce latency
    - Student initialization on teacher's ODE trajectories
    - Asymmetric distillation strategy
      - i.e.) Supervise a causal student model with a bidirectional teacher
      - Authors argue that this mitigates the error accumulation in AR generation.


---

<br><br>

## 4. Methods

### 4.1 AR
- Desc.)
  - 3D VAE Encoder
    - Compress video frames into shorter chunks of latent frames
  - Latent Diffusion
    - Attention
      - Block(Chunk)-wise causal attention
      - Within each chunk, apply bidirectional self attention
        - Masking Implementation : $$M_{i,j} = \begin{cases} 1 & \text{if } \left\lfloor\frac{j}{k}\right\rfloor \le \left\lfloor\frac{i}{k}\right\rfloor \\ 0 & \text{otherwise} \end{cases}$$
          - $$i,j$$ index the frames in the sequence
          - $$k$$ is the chunk size
    - $$G_\phi$$ : DiT for AR video generation

<br>

### 4.2 Bidirectional to Causal Generator Distillation
- Desc.)
  - $$\left\{ x_t^i \right\}_{i=1}^L$$ : $$N$$ noisy input frames divided in to $$L$$ chunks where $$i=1,\ldots,L$$
  - Diffusion Forcing
    - Each chunk $$\left\{ x_t^i \right\}$$ has its own noise timestep $$t^i\sim[0,999]$$
  - Asymmetric Distillation
    - How?)
      - Employ bidirectional attention in the teacher model
      - Constrain the student model to causal attention
    - Why doing this?)
      - Causal diffusion models underperform bidirectional ones.
        - cf.) Causal Diffusion
          - Convert pretrained bidirectional DiT into causal diffusion teacher
          - Fine-tune with denoising loss
      - AR teacher's error accumulation inherits to AR student.
    - Loss)   
      $$\begin{aligned}
        \nabla_\phi\mathcal{L}_{\text{DMD}} &= \triangleq \mathbb{E}_t \left( \nabla_\phi \text{KL}(p_{\text{gen}, t} \Vert p_{\text{data}, t}) \right) \\
        &\approx -\mathbb{E}_t \left( \int s_{\text{data}}(\Psi(G_\phi(\epsilon), t), t) - s_{\text{gen}, \xi}(\Psi(G_\phi(\epsilon), t), t)\frac{\text{d}G_\phi(\epsilon)}{\text{d}\phi} \text{d}\epsilon \right)
      \end{aligned}$$

{% include figure.liquid path="assets/img/blog/260903_causvid/001.png" class="img-fluid rounded z-depth-1 blog-img-medium" zoomable=true %}

<br>

### 4.3 Student Initialization
- How?)
  1. Sample a $$L$$-length noise inputs from the standard Gaussian $$\mathcal{N}(0,\mathbf{I})$$.
  2. Simulate the reverse diffusion process with an ODE solver using the pre-trained bidirectional teacher model.
     - i.e.) Sample $$\left\{ x_t^i \mid i=1,\ldots,L;\; t=T,\ldots,0 \right\}$$
  3. Select a subset of $$t$$ values that match those used in the student generator $$G_\phi$$.
  4. Optimization Target : $$\mathcal{L}_{\text{init}} = \mathbb{E}_{x, t^i}\left\Vert G_\phi\left( \left\{ x_{t^i}^i \right\}_{i=1}^N , \left\{ t^i \right\}_{i=1}^N \right) - \left\{ x_{0}^i \right\}_{i=1}^N \right\Vert$$

<br>

### 4.4 KV Caching during Inference

{% include figure.liquid path="assets/img/blog/260903_causvid/002.png" class="img-fluid rounded z-depth-1 blog-img-medium" zoomable=true %}
